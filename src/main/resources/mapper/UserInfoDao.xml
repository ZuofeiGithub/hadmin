<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jili.hadmin.dao.UserInfoDao">

    <resultMap type="com.jili.hadmin.entity.UserInfo" id="UserInfoMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="username" column="username" jdbcType="VARCHAR"/>
        <result property="password" column="password" jdbcType="VARCHAR"/>
        <result property="businessid" column="businessId" jdbcType="INTEGER"/>
        <result property="imgurl" column="imgUrl" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="UserInfoMap">
        select
          id, username, password, businessId, imgUrl
        from catshopkeeper.user_info
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="UserInfoMap">
        select
          id, username, password, businessId, imgUrl
        from catshopkeeper.user_info
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="UserInfoMap">
        select
          id, username, password, businessId, imgUrl
        from catshopkeeper.user_info
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="username != null and username != ''">
                and username = #{username}
            </if>
            <if test="password != null and password != ''">
                and password = #{password}
            </if>
            <if test="businessid != null">
                and businessId = #{businessid}
            </if>
            <if test="imgurl != null and imgurl != ''">
                and imgUrl = #{imgurl}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into catshopkeeper.user_info(username, password, businessId, imgUrl)
        values (#{username}, #{password}, #{businessid}, #{imgurl})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update catshopkeeper.user_info
        <set>
            <if test="username != null and username != ''">
                username = #{username},
            </if>
            <if test="password != null and password != ''">
                password = #{password},
            </if>
            <if test="businessid != null">
                businessId = #{businessid},
            </if>
            <if test="imgurl != null and imgurl != ''">
                imgUrl = #{imgurl},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from catshopkeeper.user_info where id = #{id}
    </delete>

</mapper>